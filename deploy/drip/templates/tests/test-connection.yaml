apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drip.fullname" . }}-test-connection"
  labels:
    {{- include "drip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
